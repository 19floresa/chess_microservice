{
  "name": "chess_microservice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npx tsc --build && node main.js",
    "test": "vitest",
    "build": "npx tsc --build",
    "clean": "rm ./src/*.js *.js && clear"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "next": "^16.0.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "vitest": "^4.0.9"
  }
}
